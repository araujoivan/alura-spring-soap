Create a client which uses a local wsdl in resources/wsdl/users.wsdl to generate domain classes.

Att. 1) Uncomment pom.xml if you want to generate the classes again
     2) Run the spring-soap-server project before testing the client
     3) Ex: GET to http://localhost:8096/get-user/Sam
     